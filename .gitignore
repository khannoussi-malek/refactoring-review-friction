# ── External upstream repo — NOT committed. Clone it yourself (see README):
#     git clone https://github.com/apache/hadoop.git
hadoop/

# ── Regenerable via scripts/setup.sh ──
.venv/
RefactoringMiner/
RefactoringMiner-src/
_rm_unzip/

# ── Large / intermediate RefactoringMiner output (re-detect with run_rm_safe.sh) ──
refminer*.json
rm_chunks/

# ── Jira caches (re-fetchable) — all .jira_* dirs ──
.jira_*/

# ── Git-derived caches (regenerable; gitignore has no trailing comments, keep bare) ──
# module_commit_log.json   : social_centrality.py, ~6 MB
# ticket_first_commit.json : temporal_trend.py
# ticket_change_size.json  : ci_rework.py
module_commit_log.json
ticket_first_commit.json
ticket_change_size.json

# ── Local config (may hold project-specific choices) ──
config.env

# ── OS / misc ──
.DS_Store
*.log

# NOTE: the SMALL derived results ARE committed on purpose — they are the findings:
#   architectural_episodes*.json  review_signal*.json  episode_outcomes.json  cox_dataset.json
refminer_sui*.json
prs_all.json
pr_review_detail.json
